local M = {}

function M.highlights(_colors, _set)
    -- This file is now for miscellaneous highlights that don't belong to specific plugins
    -- Most plugin-specific highlights have been moved to their own files
end

return M
